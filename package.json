{
  "name": "location-finder-api",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "devstart": "set NODE_ENV=development && babel-watch src/app.js",
    "start": "set NODE_ENV=production && npm run build && node src/bin/www",
    "build": "rm -rf dist && mkdir dist && babel src -s -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "victor awotidebe",
  "license": "ISC",
  "dependencies": {
    "@babel/runtime": "^7.10.2",
    "dotenv": "^8.2.0",
    "express": "^4.17.3",
    "pg": "^8.2.1",
    "pg-hstore": "^2.3.3",
    "sequelize": "^5.21.12"
  },
  "devDependencies": {
    "@babel/cli": "^7.10.1",
    "@babel/core": "^7.10.2",
    "@babel/plugin-transform-runtime": "^7.10.1",
    "@babel/preset-env": "^7.10.2",
    "@babel/register": "^7.10.1",
    "babel-watch": "^7.0.0",
    "sequelize-cli": "^5.5.1"
  }
}
